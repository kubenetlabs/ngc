export default function RouteList() {
  return (
    <div>
      <h1 className="text-2xl font-bold">Routes</h1>
      <p className="mt-2 text-muted-foreground">Manage HTTP, gRPC, TLS, TCP, and UDP routes.</p>
    </div>
  );
}
